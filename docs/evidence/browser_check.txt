2026-09-25 内蔵ブラウザ（Chromium系）で http://localhost:8080/ を確認。Leaflet + OpenStreetMap 版のみ。Google 版は未検証。
【マーカー数 = API の件数】
  京都: API 8 / 画面のマーカー 8 / DOM の .leaflet-marker-icon 8  → 一致
  札幌: API 4 / マーカー 4 / DOM 4（DB には5件。「交通」は札幌で非表示の設定のため1件は出ない）→ 一致
  那覇: API 5（curl で確認）
  カテゴリ選択: 飲食のみ／全て外す(0件)／全て戻す(8件) → 画面・API・DOM が一致。マーカーを押すと情報ウィンドウ（名前・カテゴリ・住所・説明）。帰属表示「© OpenStreetMap contributors」あり
【見つけた不具合（再現→修正→再確認）】
  再現: カテゴリのチェックを連続で外すと、古い応答が後から届き、飲食のみの表示が 4件（正しくは 2件）になった。画面の「一致」表示は、古い応答どうしの一致なので気付けなかった
  修正: 最新の要求の結果だけを使う（public/assets/js/map.js の refresh）。ブラウザ用ファイルには更新時刻のバージョンを付けて、古いキャッシュを使わない
  再確認: 「全部チェック → 飲食以外を連続で外す」を10回繰り返し、毎回 UI の件数 = 直接呼んだ API の件数(2) = DOM のマーカー数 → 10/10 一致
【その他】コンソールエラー 0件。「Google Maps（APIキー未設定・未検証）」の選択肢は無効
【未確認】他のブラウザ・スマホ実機・管理画面の目視
