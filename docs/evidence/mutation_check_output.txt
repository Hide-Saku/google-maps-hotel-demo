== 正常版 ==
  合格 27 / 失敗 0  -> GREEN
== 壊す1: 認可チェックを外す（未ログインでも管理画面に入れる） ==
  合格 18 / 失敗 9  -> RED（検知できた）
    FAIL 未ログインで管理画面を開くと、ログイン画面へ飛ばされる
    FAIL ログイン: CSRF なし・間違ったトークンは 403、間違ったパスワードは拒否
    FAIL 施設の追加: CSRF なし・違うトークンは 403で件数不変、正しいトークンで追加される
    FAIL 管理画面の入力に SQL の特殊文字を入れても壊れず、API がそのまま返す
== 壊す2: CSRF 検証を外す（トークンなしの POST が通る） ==
  合格 23 / 失敗 4  -> RED（検知できた）
    FAIL ログイン: CSRF なし・間違ったトークンは 403、間違ったパスワードは拒否
    FAIL 施設の追加: CSRF なし・違うトークンは 403で件数不変、正しいトークンで追加される
    FAIL 施設の削除: POST＋CSRF のときだけ。GET では消えない
    FAIL ログアウトは POST＋CSRF のときだけ。その後は管理画面に入れない
== 壊す3: 冪等の既存チェックを外す（同じデータを流すと必ず追加しようとする） ==
  合格 25 / 失敗 2  -> RED（検知できた）
    FAIL 2回目（同じデータ）: 件数が増えない（冪等）
    FAIL 内容が変わった行だけ更新される（追加はされない）
== 壊す4: SQL をプレースホルダでなく文字列連結にする（ホテル検索） ==
  合格 26 / 失敗 1  -> RED（検知できた）
    FAIL ホテルのスラッグに SQL を入れても、全件は返らない
== 壊す5: HTML エスケープをやめる（h() がそのまま返す） ==
  合格 26 / 失敗 1  -> RED（検知できた）
    FAIL XSS: 管理画面は施設名の HTML をエスケープして出す
== 壊す6: ログイン成功時にセッションIDを作り直さない（固定化攻撃） ==
  合格 26 / 失敗 1  -> RED（検知できた）
    FAIL ログイン成功でセッションIDが変わる（固定化攻撃への対策）／クッキーは HttpOnly・SameSite
== 壊す7: 地図に出す施設の絞り込み（有効カテゴリ）を外す ==
  合格 26 / 失敗 1  -> RED（検知できた）
    FAIL 地図に出すのは、そのホテルで有効なカテゴリの施設だけ（札幌は「交通」が無効）

検知できなかった変異: 0 件
